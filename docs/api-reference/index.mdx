---
title: "API Reference"
description: "REST and WebSocket API for markets, prices, and trades."
---

# Coming soon

The public floorlaunch API: markets, candles, trades, the floor index aggregator, and a WebSocket stream: is being finalized alongside the hosted indexer.

Until then:

- The **program reference** (accounts, instructions, PDAs, events, errors) is complete under [Documentation → Program Reference](/reference/instructions).
- Everything the app shows comes from the same indexer endpoints the API will expose, so the shapes you see in the app are the shapes you will get.
